# Hook up this virtual pin in the motion component to our physical pin
# on the parallel port breakout board. Now M62-M65 (with P0) will
# trigger the attached relay and turn on the laser crosshairs.
net laser motion.digital-out-00 => parport.0.pin-16-out

# Invert because my relays switch when the input is driven low.
setp parport.0.pin-16-out-invert 1
